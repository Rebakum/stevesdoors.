import { Register } from "@/Components/Auth/Register/Register";

const registerPage = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-gray-100 py-60">
      <Register />
    </div>
  );
};

export default registerPage;
